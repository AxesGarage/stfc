version: "3.7"

services:
  php:
    image: stfc_prod:latest
    restart: always

  nginx:
    build:
      context: ../images
      dockerfile: nginx.Dockerfile
    restart: always
    depends_on:
      - php
    ports:
      - "8002:80"
